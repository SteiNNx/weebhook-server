{
  "name": "local-webhook",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node src/start.js",
    "test": "nyc --all --reporter=lcov --reporter=text mocha --timeout 5000 --recursive test"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.0.0",
    "nyc": "^15.1.0",
    "supertest": "^6.1.6"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.18.2"
  }
}